"""Canonical depth combine / ratio-stat mode names for map and sample."""

# Order: min, mean, median, max, random (keep consistent across CLI, docs, and validation).
DEPTH_MODES = ("min", "mean", "median", "max", "random")
